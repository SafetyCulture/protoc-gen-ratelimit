-- Code generated by protoc-gen-ratelimit. DO NOT EDIT.

local M = {}

function M.get_ratelimit_bucket(path, method)
  {{- range $method, $paramMap := .}}
  if method == "{{ $method }}" then
    {{- range $hasParams, $paths := $paramMap}}
    {{- range $paths}}
    {{- if $hasParams}}
    if path:match('^{{ .Path }}$') then
      return "{{ .Bucket }}"
    end
    {{- else}}
    if path == "{{ .Path }}" then
      return "{{ .Bucket }}"
    end
    {{- end}}
    {{- end}}
  {{- end}}

    return "other"
  end
{{- end}}

  return "other"
end

return M
